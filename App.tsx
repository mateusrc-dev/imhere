import { Home } from "./src/screens/Home";

export default function App() { // aqui é obrigatório ter o default porque é o arquivo App (primeiro a ser procurado pra ser renderizado)
  return (
    <Home />
  );
}
